name: specformer
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.0.0
  - cudatoolkit=11.8
  - pip
  - pip:
    - transformers>=4.35.0
    - einops>=0.7.0
    - fair-esm>=2.0.0
    - wandb>=0.16.0
    - pyyaml>=6.0
    - tqdm>=4.65.0
